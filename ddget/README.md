# HELP
```
usage: ddget [-h] [--query QUERY]

Datadog CLI.

optional arguments:
  -h, --help     show this help message and exit
  --query QUERY  query to filter metrics: system.cpu.idle{*}by{host}
```